import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uz.blockpuzzle.game',
  appName: ' Block Puzzle Game ABDUKADIR_ZAFAR',
  webDir: 'public'
};

export default config;
